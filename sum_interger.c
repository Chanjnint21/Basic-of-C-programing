//write code to sum two interger by input one time

#include <stdio.h>
int main(){
  int num1, num2, sum;
  printf ("Enter two Intergers you want to sum:");
  scanf("%d""%d", &num1, &num2);
  sum = num1 + num2;
  printf("Add number = %d",sum);
  return 0;
}